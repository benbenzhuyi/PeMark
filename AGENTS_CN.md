# AGENTS_CN.md — 强制性开发指令（中文版）

> **语言要求：** 所有面向用户可见的 Agent 推理、计划、进度说明、测试结论和最终汇报，必须使用**简体中文**。代码符号、命令、API 名称、文件名、标识符以及必要的英文技术专有名词保持原样。
>
> **文档语言：** 英文是项目文档的规范源语言。需要中英文双语版本的项目文档，先更新英文原文，再同步 `.zh-CN.md` / 中文版。该规则**不覆盖**"所有面向用户可见的 Agent 沟通必须使用简体中文"的要求。

## 1. 项目身份

本项目是一个 **纯 Direct-PE Windows x64 实验**。构建时由 Python 生成器直接构造 PE32+ 镜像、导入表、静态数据、虚拟 BSS 以及 AMD64 机器码字节。

本项目的目标不仅是交付一个 Markdown 编辑器，同时也是一个"AI 辅助直接机器码软件开发"的工程实验。任何架构和实现决策都必须保留这一研究属性。

### 不可协商的构建约束

生产 `.exe` **不得**由以下方式生成：

- C/C++ 编译器
- Rust 编译器
- 汇编器
- 链接器
- .NET / 托管编译器
- 内嵌解释器的打包器

分析和调试工具允许使用。Python 允许作为构建期生成器和测试框架。

## 2. 工作原则：上下文是有限的工程资源

必须把模型上下文视为**硬性工程预算**。

不得因为"工具可用"就追求最大化阅读、搜索和调用次数。始终优先使用足以完成并验证当前改动的最小上下文。

### 上下文节流工作流

1. 每次只处理**一个边界明确的工程切片**。
2. 除非已有明确失败证据要求，否则不得扩大任务范围。
3. 当前任务摘要已经确认的文件、文档、日志和结论，除非出现新证据，否则不得重复读取或重新审计。
4. 优先使用 `rg`、`git diff`、`git log`、精确符号搜索和局部行读取，避免整文件读取。
5. 不得仅为了"理解项目"而完整读取大型 generator 或大型文档。
6. 优先运行最相关的已有测试；只有出现具体失败后，才展开读取对应实现。
7. 控制命令输出规模。优先使用过滤结果、精确匹配、摘要和失败片段，避免把大段日志塞入上下文。
8. 没有新证据时，不得反复搜索同一符号或重新打开相同文件。
9. 一个完整工程切片实现并验证后，**立即停止并汇报检查点**，不得自动开始新的工程切片。
10. 在开始新的大范围调查、读取大型文件、无关子任务或架构扩展之前，先形成一个精简检查点，只保留：
    - 当前目标
    - 已完成修改
    - 验证结果
    - 未完成的下一步
    - 关键文件
    - 关键符号 / label
11. 如果宿主因为工具调用 / 思考轮次上限而停止执行，应从已有检查点继续。不得重新审计已经完成的工作。
12. 不得假设模型能够看到"剩余工具轮数"或宿主侧的精确上下文百分比。不得建立依赖这些不可见数值的计划。

### 停止条件

出现以下任一情况，应停止当前切片并汇报，而不是继续扩展：

- 请求的改动已经实现且相关验证通过；
- 下一步需要进入新的子系统或无关调查；
- 证据不足以支持扩大当前假设；
- 当前故障已经完成因果隔离，需要用户决定是否进入下一阶段；
- 宿主由于自身执行上限中止当前任务。

## 3. 编辑前

**不得**在每次改动前自动重新读取全部项目文档。

默认启动顺序：

1. 阅读本 `AGENTS.md`。
2. 如果存在当前任务摘要 / handoff，读取它。
3. 检查 `git status` 和与当前任务相关的 `git diff`。
4. 定位当前任务涉及的精确符号、测试和文件。
5. 只读取当前任务直接需要的最小源码范围或项目文档。

只有当当前改动确实涉及对应问题时，才读取架构、里程碑、ABI、审计、失败方案或发布文档。

相关项目文档包括但不限于：

- `docs/SYSTEM_ARCHITECTURE_LONG_TERM.md`
- `docs/MILESTONE_PLAN.md`
- `docs/DEVELOPMENT_MANUAL_V8_5_PLUS.md`
- `docs/CURRENT_CODE_AUDIT.md`
- `docs/WIN64_ABI_RULES.md`
- `docs/ARCHITECTURE_V8_5_TARGET.md`
- `docs/FAILED_APPROACHES.md`

**不得默认整套全部读取。**

只有以下情况才重新构建并计算当前 baseline 哈希：

- 当前任务或验证级别明确要求；
- baseline 身份不明确；
- 当前正在验证确定性构建；
- 回归对比需要一个已知 baseline。

文档发生冲突时，以长期架构、当前里程碑计划和当前开发手册为准。历史审计/交接文件是证据，不自动等同于当前策略。

## 4. 硬性工程规则

1. **单一数据源：** `DocumentModel` 拥有文档文本。
2. **单一视图模式状态：** SOURCE/PREVIEW 不得由多个独立 flag 或可见性假设重复表达。
3. **Outline 只保存 source offset。** Preview offset 必须通过 `PositionMap` 派生。
4. **Win64 易失寄存器：** 每次 Win64 API 调用都可能破坏 `RAX`、`RCX`、`RDX`、`R8`、`R9`、`R10`、`R11`。不得在这些寄存器中跨调用保存活跃值。
5. 从生成的机器代码执行每一次嵌套调用前，必须维护 Win64 栈对齐和 32 字节 shadow space。
6. 把 stack frame 边界视为契约。不得写出已分配的局部栈空间，不得把保存的返回地址槽当临时变量。
7. 必须明确区分"地址"和"内容"。将 BSS/静态内联对象按指针传给 API 时，除非 API 确实需要对象内容，否则必须使用取地址形式（`lea` 类语义）。
8. 几何属于唯一的 `LayoutManager`。不得让各个命令独立计算或移动面板。
9. 滚动条绘制、命中测试和拖拽必须使用同一个缓存的 `thumbRect`；不得各算一套略有差异的矩形。
10. 主题切换只允许切换 Palette + invalidate；不得重新解析 Markdown。
11. 缩放不得重建 Markdown `RenderModel`。
12. 大文档不得执行 RichEdit 全文逐 span 格式化。
13. 未经 Windows 实机执行，不得声称 GUI bug 已修复。静态检查有价值，但不充分。
14. 每个真实回归修复，在存在实际且成本合理的防回退方式时，必须新增或更新确定性回归用例。
15. **最小充分工程：** 只实现解决已证实问题所需的最小机制。计划和架构文档描述意图，不授权提前建设未来基础设施。
16. 新增检查、契约、ADR、门禁、fixture 或诊断工具之前，先说明它防止什么失败，以及为什么更便宜的方案不够。
17. **按级别设门：** 按当前开发手册执行提交级、里程碑级或发布级验证。不得对每个小改动都套用发布级流程。
18. **因果隔离优先于系统级假设：** 出现回归时，先检查最近的相关 diff，并执行最小 A/B 短路或等效的因果隔离。在没有可证伪、基于证据的具体假设之前，不得扩展到 PE loader、TLS、BSS、DPI、USER32 初始化或其他系统级假设。
19. 一小段错误代码，不构成项目级调试远征的理由。
20. 一旦 bug 已被隔离，立即停止扩大诊断范围。修复已确认原因、完成验证、增加成本最低且有价值的防回退措施，然后回到路线图。

## 5. 回归与调试纪律

出现回归时，按以下顺序执行：

1. 确定第一个已知失败行为和最近一个已知正常证据。
2. 检查最近的相关 diff。
3. 提出一个具体、可证伪的假设。
4. 做能够确认或证伪该假设的最小 A/B 实验。
5. 如果确认，只修复该原因。
6. 先运行最窄的相关回归。
7. 窄测试通过后，再按需扩大测试集合。
8. 只有当新增回归 fixture/assertion 的成本低于再次复发的成本时，才将其永久化。
9. 临时诊断脚本不再需要时应删除或归档。

禁止：

- 在简单因果实验尚未执行前连续编写多个大型诊断脚本；
- 最近本地代码改动尚未被验证时，直接研究 loader/OS 内部；
- 没有理由地反复运行已经通过的宽泛测试套件；
- 用中间失败构建推断无关子系统存在问题。

## 6. 工具与文件读取纪律

### 默认优先顺序

1. `git status`
2. `git diff -- <relevant path>`
3. 精确符号 / 精确字符串搜索
4. 局部源码范围读取
5. 运行相关测试
6. 只有仍然需要时才扩大检查范围

### 默认避免

- 整文件读取大型 generator；
- 用模糊概念进行全项目搜索；
- 处理局部代码修复时读取 roadmap/changelog/release notes；
- 将完整日志直接放入上下文；
- 重新打开已经被当前检查点摘要过的文件；
- 除非任务就是翻译一致性，否则不要同时读取同一文档的英文版和中文版。

## 7. 变更纪律

- 优先有边界的重构，而不是补丁链。
- 每个状态只能有一个明确拥有它的子系统。
- 工作过程中维护 `CHANGELOG_UNRELEASED.md`，但只有当前切片确实需要 changelog 时才更新。
- 需要双语发布的新项目文档，必须提供英文版和简体中文版，并在顶部互相链接。
- 修改 PE 布局时，重新生成 `docs/PE_MEMORY_MAP.md` 并运行 `tools/inspect_pe.py`。
- 修改 imports 时，更新 `docs/WIN32_API_SURFACE.md`。
- 修改快捷键 / 菜单命令时，更新 `docs/FEATURE_AND_SHORTCUT_SPEC.md`。
- 不得为了让某个目标测试通过而修改无关测试。
- 未经明确指示，不得 reset、rebase、clean、丢弃或覆盖用户/其他 Agent 的无关工作。
- 不得 force-push。
- 将未提交工作视为有价值状态。

## 8. 验证策略

验证范围必须与改动范围匹配。

### 提交级

使用能够证明当前改动正确的最窄验证。

典型示例：

- generator assertions
- 定向单元 / 静态回归
- 定向 Windows 运行时测试
- 仅在相关时进行确定性构建 / hash 验证

### 里程碑级

完成里程碑切片时，运行该里程碑定义的更广泛回归套件。

### 发布级

只有在准备发布或用户明确要求时，才运行完整 release gate。

不得在每个小修复后运行历史完整发布套件。

涉及 GUI / runtime 的正确性声明，必须有 Windows 实机执行证据。

## 9. 工程切片结束与检查点格式

每个工程切片结束时，用简体中文简短汇报：

```text
当前目标：
- ...

已完成：
- ...

验证：
- ...

未完成 / 下一步：
- ...

关键文件：
- ...

关键符号：
- ...
```

汇报完成后，不得自动启动下一个工程切片；除非用户已经明确要求连续执行，并且下一切片仍在同一个有边界的任务范围内。

如果之后由宿主执行 Compact / 上下文压缩，则该检查点是恢复工作的权威种子。不得从头重新建立整个项目理解。

## 10. 仓库卫生

临时诊断文件必须：

- 名称明确标记为临时；
- 不放入生产路径；
- 问题解决后删除或归档；
- 后续正常任务不再把它们加入上下文，除非确实再次需要。

不得把一次调试远征遗留成永久项目复杂度。

## 11. 沟通规则

所有面向用户可见的沟通必须使用简体中文，包括：

- 推理摘要
- 计划
- 进度说明
- 工具调用说明
- 测试结论
- 故障分析
- 最终汇报

例外：

- 源代码
- 命令
- API 名称
- 文件名
- 标识符
- 必须原样引用的错误信息

不得因为源码、Windows API 或工具输出是英文，就使用英文自然语言进行说明。

表达必须简洁、基于证据，并明确区分：

- 已观察事实
- 推断
- 假设
- 已确认根因
- 尚未验证的假设

在因果实验通过之前，不得把假设描述为"已确认根因"。
