# PeMark V8.6 (unreleased)

V8.5.4 shipped as the first stable release; its full record is
`docs/CHANGELOG_V8_5_4.md`.

V8.6 is the workspace milestone: a safe multi-file navigation layer (directory
tree, recent files, drag-and-drop open, create/rename/delete behind the existing
unsaved-change protection, window state persistence, external-change detection)
while the application still owns a single writable document.

Add entries here as each bounded change lands, with level-appropriate evidence.
